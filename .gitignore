# Eclipse files
bin/
.classpath
.project
.settings

# the gradle temp dir
.gradle

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# gradle's output dir
build/
